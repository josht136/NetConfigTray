<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net9.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>NetConfigTray</AssemblyName>
    <RootNamespace>NetConfigTray</RootNamespace>
    <Version>1.0.0</Version>
    <Description>Tiller Network Tool (TNT) — network interface IP and DHCP status</Description>
    <Company>Tiller</Company>
    <Product>Tiller Network Tool</Product>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <UseAppHost>false</UseAppHost>
    <PublishSingleFile>true</PublishSingleFile>
    <SelfContained>false</SelfContained>
  </PropertyGroup>

  <!-- Re-enable apphost (.exe) only when publishing a release build -->
  <PropertyGroup Condition="'$(PublishProtocol)' != '' or '$(RuntimeIdentifier)' != ''">
    <UseAppHost>true</UseAppHost>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Management" Version="9.0.0" />
    <PackageReference Include="System.IO.Ports" Version="9.0.0" />
    <PackageReference Include="SSH.NET" Version="2025.0.0" />
  </ItemGroup>

</Project>
